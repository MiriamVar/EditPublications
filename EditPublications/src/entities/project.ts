import { GantScheme } from 'src/modules/forms/form/form.component';

export class Project{
    // public constructor(
        public nameP: string = '';
        public numberP: string = '';
        
        // public scheme: GantScheme = new GantScheme(),
        public scheme: string= '';
        public agency: string ='';
        id: number ;
    // ) {}
}